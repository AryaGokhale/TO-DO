version: '3'
services:
  app:
    container_name: todo_project

    build: 
      context: .
      dockerfile: Dockerfile
    
    ports: 
      - 8080:8080 
    
    # restart: on-failure
    
# volumes:
#   - api:/usr/src/app/